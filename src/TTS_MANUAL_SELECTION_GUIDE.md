# 🎛️ TTS Manual Selection Guide

## Руководство по ручному выбору TTS движков

Система ручного выбора TTS движков позволяет пользователю настраивать предпочтительные TTS движки для каждого языка отдельно.

## 🚀 Возможности

### ✅ Автоматический выбор
- Система автоматически выбирает лучший TTS движок для каждого языка
- Рекомендации основаны на качестве, скорости и доступности

### ⚙️ Ручная настройка
- Возможность выбрать предпочтительный TTS движок для любого языка
- Настройка fallback движка на случай недоступности основного
- Выбор конкретного голоса (если поддерживается движком)
- Заметки для настроек

### 🎤 Доступные TTS движки

1. **macOS System TTS**
   - ✅ Качество: 8/10
   - ⚡ Скорость: 9/10  
   - 💰 Стоимость: Бесплатно
   - 🌍 Языки: ru, uk, en, de, fr, es, it и др.

2. **Google Text-to-Speech**
   - ✅ Качество: 7/10
   - ⚡ Скорость: 6/10
   - 💰 Стоимость: Бесплатно (с ограничениями)
   - 🌍 Языки: 30+ языков

3. **ElevenLabs TTS** (планируется)
   - ✅ Качество: 10/10
   - ⚡ Скорость: 4/10
   - 💰 Стоимость: API ключ (платно)
   - 🌍 Языки: en, ru, de, fr, es и др.

## 📊 Рекомендации по языкам

- **🇷🇺 Русский**: macOS TTS (Milena) - лучшее качество
- **🇺🇦 Украинский**: Google TTS - надежнее чем Lesya
- **🇺🇸 Английский**: macOS TTS - естественное звучание
- **🇩🇪 Немецкий**: macOS TTS (Anna) 
- **Остальные языки**: Google TTS - широкая поддержка

## 🛠️ Использование

### API Endpoints

1. **Получить доступные движки**
   ```
   GET /api/tts/engines
   ```

2. **Получить движки для языка**
   ```
   GET /api/tts/engines/{language}
   ```

3. **Установить настройки**
   ```
   POST /api/tts/preference
   {
     "language": "ru",
     "engine": "macos",
     "voice": "Milena",
     "fallback_engine": "google_tts",
     "notes": "Предпочтение пользователя"
   }
   ```

4. **Получить настройки языка**
   ```
   GET /api/tts/preference/{language}
   ```

5. **Удалить настройки**
   ```
   DELETE /api/tts/preference/{language}
   ```

6. **Тестировать TTS**
   ```
   POST /api/tts/test
   {
     "language": "ru",
     "engine": "macos",
     "text": "Тест синтеза речи"
   }
   ```

### Веб-интерфейс

Откройте `tts_interface_test.html` в браузере для графического интерфейса:
- 📊 Просмотр доступных движков
- ⚙️ Настройка предпочтений для языков
- 🎤 Тестирование TTS синтеза
- 📋 Управление настройками

### Программный интерфейс

```python
from core.tts_manual_selector import tts_manual_selector
from core.tts_engine_factory import TTSEngine

# Установить предпочтение
tts_manual_selector.set_user_preference(
    language='ru',
    engine=TTSEngine.MACOS,
    voice='Milena',
    fallback_engine=TTSEngine.GOOGLE_TTS,
    notes='Лучшее качество для русского'
)

# Получить эффективный движок
engine = tts_manual_selector.get_effective_engine_for_language('ru')
print(f"Используется: {engine.value}")

# Синтез с настройками
from core.speech_synthesizer import SpeechSynthesizer
synthesizer = SpeechSynthesizer()
result = synthesizer.synthesize_speech("Привет", "ru")
```

## 📁 Файлы системы

- `core/tts_engine_factory.py` - Фабрика TTS движков
- `core/tts_manual_selector.py` - Ручной селектор настроек
- `core/tts_performance_tester.py` - Тестирование производительности
- `core/speech_synthesizer.py` - Обновленный синтезатор (интегрирован)
- `web_app.py` - API endpoints (добавлены)
- `logs/tts_user_preferences.json` - Пользовательские настройки

## 🔧 Настройки по умолчанию

Если пользовательские настройки не заданы, система использует:

```
🇷🇺 ru: macOS TTS (Milena)
🇺🇦 uk: Google TTS  
🇺🇸 en: macOS TTS (Samantha)
🇩🇪 de: macOS TTS (Anna)
🇫🇷 fr: macOS TTS (Amelie)
🇪🇸 es: macOS TTS (Monica)
Другие: Google TTS
```

## 🚨 Обработка ошибок

- ✅ Автоматический fallback на резервный движок
- ✅ Проверка доступности движков
- ✅ Валидация языков и голосов
- ✅ Graceful degradation при ошибках

## 🎯 Примеры использования

### Пример 1: Настройка для русского
```bash
curl -X POST http://localhost:5000/api/tts/preference \
  -H "Content-Type: application/json" \
  -d '{
    "language": "ru",
    "engine": "macos", 
    "voice": "Milena",
    "fallback_engine": "google_tts",
    "notes": "Лучшее качество"
  }'
```

### Пример 2: Тест украинского TTS
```bash
curl -X POST http://localhost:5000/api/tts/test \
  -H "Content-Type: application/json" \
  -d '{
    "language": "uk",
    "engine": "google_tts",
    "text": "Привіт, це тест TTS"
  }'
```

### Пример 3: Просмотр всех настроек
```bash
curl http://localhost:5000/api/tts/preferences
```

## 🔄 Обновления

Система автоматически:
- ✅ Сохраняет настройки в JSON файл
- ✅ Загружает настройки при запуске
- ✅ Обновляет эффективные движки при изменениях
- ✅ Логирует все операции

---

**Создано:** 2025-09-28  
**Версия:** 1.0  
**Совместимость:** Video-Translator v2.0+