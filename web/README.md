# 🌐 Web Interfaces

Веб-интерфейсы для Video-Translator.

## 📁 Файлы

### `index.html`
- **Назначение**: Основной веб-интерфейс для загрузки и перевода видео
- **Функции**: 
  - Загрузка видеофайлов
  - Настройка параметров перевода
  - Мониторинг процесса перевода
  - Скачивание результатов

### `tts_interface.html`
- **Назначение**: Интерфейс для ручного выбора TTS движков
- **Функции**:
  - Просмотр доступных TTS движков
  - Настройка предпочтений для разных языков
  - Выбор голосов и резервных движков
  - Тестирование TTS синтеза
  - Управление настройками

## 🚀 Использование

### Запуск единого сервера
Запустите основной сервер (содержит все API):
```bash
cd ../src
python web_app.py
```

**Примечание**: Сервер содержит все функции - и основной интерфейс, и TTS API.

### Открытие интерфейсов
1. **TTS Interface**: Откройте `tts_interface.html` в браузере
2. **Main Interface**: Перейдите на http://127.0.0.1:5000 (если запущен web_app.py)

## 🔧 API Endpoints

TTS интерфейс использует следующие API endpoints:

- `GET /api/tts/engines` - Список доступных движков
- `GET /api/tts/engines/{language}` - Движки для языка
- `GET /api/tts/preferences` - Все настройки
- `POST /api/tts/preference` - Установить настройки
- `DELETE /api/tts/preference/{language}` - Удалить настройки
- `POST /api/tts/test` - Тестировать TTS
- `POST /api/tts/reset` - Сбросить все настройки

## 📝 Примечания

- TTS интерфейс работает с API на порту 5000
- Основной интерфейс также использует порт 5000
- Убедитесь что соответствующий сервер запущен перед использованием
- Настройки TTS сохраняются в `logs/tts_user_preferences.json`